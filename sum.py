# I am using the method named sum which represents addition
# In Python: this method is already built-in and pre-defined
# That is the reason because we directly write sum

numbers = [1,2,3,4,5]

total_sum = sum(numbers)

print("Sum of the numbers: ", total_sum)


